import 'package:flutter/material.dart';

class ChatbotHistory extends StatelessWidget {
  const ChatbotHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
